@page "/"
@using System.Threading
@using Lomboque.Application.Actions
@using Lomboque.Application.Common.Managers

@inject IHttpClientFactory ClientFactory
@inject AppController AppController

@inject ApplicationManager AppManager

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

@code{

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if(firstRender)
        {
            string uri = "http://192.168.2.101/get";
            await AppManager.HourlyTask(uri);
            System.Console.WriteLine("Bleh");

        }
    }
}